services:
    - name: "service-0"
      addr: ":443"
      listener:
          type: "tls"
          tls:
              certFile: "cert.pem"
              keyFile: "key.pem"
      handler:
          type: "http"
